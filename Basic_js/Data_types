"use  strict";//treat all JS code as newer version
//alert("hello") we are using node js , not browser

console.log(3+3); //uderstand to neat and clean code as possible to make.
console.log("abhi");//code readblity that should be high.
//primitve data
let name="Abhi"// this is string data type .string=>""(dual quotes must be suggested).
let age= 22//this number data type. number=> 2 to power 53(range),bigint.
let isLoggedIn= true // this boolean data type ture/flase. boolean(true/flase).
null //stand alone value consider this consider as object in JS
undefined// when you didn't give any value (null => empaty (not)) when you intesliy wanted to print some wihout value. OBJECT
Symbol// compnents wanted to be unqiue.

//object
console.log(typeof"abhi");
console.log(typeof 18);
console.log(typeof null);
console.log(typeof undefined);
